# Optional exact-match overrides, evaluated before the shipped app rules.
#
# Format: match|value|workspace|layout
#   match      id or name
#   workspace  a configured number, current to stay where opened, or - for a
#              layout-only override that keeps the shipped workspace default
#   layout     tiling, floating, or - to keep the current layout
#
# Examples:
# id|com.microsoft.VSCode|3|tiling
# name|Safari|6|tiling
# id|com.example.Utility|current|floating
#
# Context tools belong where they are invoked, not on a global utility desk.
id|com.apple.finder|current|floating
id|com.apple.Preview|current|floating

# Invalid records are ignored. The installer preserves local edits unless
# DOTFILES_FORCE=1 is used. Use app-route.sh for quick, backed-up changes.
